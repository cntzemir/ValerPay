# Tests

- Backend tests live in `src/backend/src/**/*.spec.ts` and `src/backend/test/*` (Jest + Nest).
- Frontend tests are planned (see Roadmap in root README).

CI runs backend unit tests + build checks for frontends.
